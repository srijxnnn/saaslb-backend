services:
  mongodb:
    image: mongo:8
    ports:
      - "27017:27017"
    volumes:
      - saaslb_mongo:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  saaslb_mongo:
